server {
       listen ##SERVER_PORT##;
       listen [::]:##SERVER_PORT##;

       server_name localhost;

       root /workspaces/csm/build/html;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}